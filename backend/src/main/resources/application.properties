# MongoDB
spring.data.mongodb.uri=mongodb+srv://linarasenarathna2001:9iJufuNRx7uUTn1D@cluster0.ttivuhw.mongodb.net/skillflow?retryWrites=true&w=majority

# OAuth2 (Google)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8081/api/auth/callback
spring.security.oauth2.client.registration.google.scope=openid,email,profile

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Frontend Redirect
frontend.redirect-url=http://localhost:3000/auth/callback

# Server Port
server.port=8081

# logging.level.org.springframework=DEBUG